	ORG	0000h
	SJMP	MAIN

	ORG	0030h
MAIN:
	MOV 	P2,	#0FFh
	MOV 	IE,	#10010000b
	MOV	PCON,	#80h
	MOV 	TMOD,	#00100000b
	MOV	TH1,	#0FAh
	MOV	R0,	P0
	MOV	R1,	P2
	SETB	TR1
	
LOOP:
	MOV	A,	P2
	XRL	A,	R1
	JZ	LOOP2
	LCALL	SERIAL_TRM
	SJMP	LOOP

LOOP2:
	MOV	A,	P0
	XRL	A,	R0
	JZ	LOOP
	LCALL	MODE_H
	SJMP	LOOP	
